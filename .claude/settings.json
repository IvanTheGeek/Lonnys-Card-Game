{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd /home/ivan/PROJECTS/LONNEY-TEST && FILE=$(jq -r '.tool_input.file_path // .tool_response.filePath') && [[ \"$FILE\" == /home/ivan/PROJECTS/LONNEY-TEST/* ]] && git add -u && ! git diff --cached --quiet && git commit -m \"auto: update $(basename $FILE)\" && git push origin || true",
            "statusMessage": "Auto-committing changes..."
          }
        ]
      }
    ]
  }
}
